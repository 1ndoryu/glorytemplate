# Code Sentinel - Reporte de Workspace

**Fecha:** 2026-02-24 21:32:55  
**Archivos analizados:** 719  
**Archivos con violaciones:** 15  
**Total violaciones:** 23  

| Severidad | Cantidad |
|-----------|----------|
| Error | 0 |
| Warning | 23 |
| Info | 0 |
| Hint | 0 |

---

## App/React/hooks/useMenuContextualPublicacion.tsx (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 107 | Warning | promise-sin-catch | .then() sin .catch() y fuera de try-catch. Los errores de la Promise se pierden. |
| 134 | Warning | promise-sin-catch | .then() sin .catch() y fuera de try-catch. Los errores de la Promise se pierden. |
| 190 | Warning | promise-sin-catch | .then() sin .catch() y fuera de try-catch. Los errores de la Promise se pierden. |

---

## App/React/components/social/ModalEditar.tsx (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 189 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |
| 204 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |
| 270 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |

---

## App/React/islands/social/EditarPerfilIsland.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 47 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |
| 68 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |

---

## App/React/components/social/ContenidoCrear.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 244 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |
| 246 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |

---

## App/React/components/social/ListaComentarios.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 146 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |
| 149 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |

---

## App/React/components/social/ModalConfiguracion.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 83 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |
| 111 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |

---

## App/React/islands/player/ReproductorIsland.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 165 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |

---

## App/React/components/ui/DropZone.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 118 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |

---

## App/React/islands/mensajes/ChatIsland.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 70 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |

---

## App/React/components/social/ChatFlotante.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 94 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |

---

## App/React/components/social/ModalColeccion.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 67 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |

---

## App/React/components/social/ModalPublicar.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 156 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |

---

## Mezclador/components/PistaTimeline.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 91 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |

---

## Mezclador/components/ChannelRack/SelectorPatron.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 73 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |

---

## Mezclador/components/MezcladorPanel.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 104 | Warning | html-nativo-en-vez-de-componente | Usar componente <Input> (o <Checkbox>/<Radio> segun type) en vez de <input> nativo. Import desde components/ui. |

