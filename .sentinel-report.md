# Code Sentinel - Reporte de Workspace

**Fecha:** 2026-02-21 03:21:57  
**Archivos analizados:** 530  
**Archivos con violaciones:** 86  
**Total violaciones:** 207  

| Severidad | Cantidad |
|-----------|----------|
| Error | 17 |
| Warning | 190 |
| Info | 0 |
| Hint | 0 |

---

## App/Kamples/Database/VerificarPgvector.php (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 167 | Error | exec-sin-escapeshellarg | exec()/shell_exec() sin escapeshellarg(). Riesgo de inyeccion de comandos. |
| 168 | Error | exec-sin-escapeshellarg | exec()/shell_exec() sin escapeshellarg(). Riesgo de inyeccion de comandos. |
| 169 | Error | exec-sin-escapeshellarg | exec()/shell_exec() sin escapeshellarg(). Riesgo de inyeccion de comandos. |
| 177 | Error | exec-sin-escapeshellarg | exec()/shell_exec() sin escapeshellarg(). Riesgo de inyeccion de comandos. |

---

## App/Kamples/Api/Controladores/ComentariosInteraccionController.php (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 186 | Warning | json-decode-inseguro | json_decode() sin verificar json_last_error(). Datos corruptos se propagan como null silencioso. |
| 222 | Error | exec-sin-escapeshellarg | exec()/shell_exec() sin escapeshellarg(). Riesgo de inyeccion de comandos. |
| 249 | Error | exec-sin-escapeshellarg | exec()/shell_exec() sin escapeshellarg(). Riesgo de inyeccion de comandos. |
| 304 | Error | exec-sin-escapeshellarg | exec()/shell_exec() sin escapeshellarg(). Riesgo de inyeccion de comandos. |

---

## App/Kamples/Api/ProcesadorFFmpeg.php (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 73 | Error | exec-sin-escapeshellarg | exec()/shell_exec() sin escapeshellarg(). Riesgo de inyeccion de comandos. |
| 157 | Error | exec-sin-escapeshellarg | exec()/shell_exec() sin escapeshellarg(). Riesgo de inyeccion de comandos. |
| 181 | Error | exec-sin-escapeshellarg | exec()/shell_exec() sin escapeshellarg(). Riesgo de inyeccion de comandos. |

---

## App/Kamples/Services/DeduplicadorAudio.php (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 173 | Error | exec-sin-escapeshellarg | exec()/shell_exec() sin escapeshellarg(). Riesgo de inyeccion de comandos. |
| 241 | Error | exec-sin-escapeshellarg | exec()/shell_exec() sin escapeshellarg(). Riesgo de inyeccion de comandos. |

---

## App/Kamples/Database/PostgresService.php (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 71 | Error | exec-sin-escapeshellarg | exec()/shell_exec() sin escapeshellarg(). Riesgo de inyeccion de comandos. |
| 72 | Error | exec-sin-escapeshellarg | exec()/shell_exec() sin escapeshellarg(). Riesgo de inyeccion de comandos. |

---

## App/Kamples/Api/PipelineAudio.php (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 118 | Error | exec-sin-escapeshellarg | exec()/shell_exec() sin escapeshellarg(). Riesgo de inyeccion de comandos. |

---

## App/Kamples/Api/DetectorTonalidad.php (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 77 | Error | exec-sin-escapeshellarg | exec()/shell_exec() sin escapeshellarg(). Riesgo de inyeccion de comandos. |

---

## App/Kamples/Api/DetectorBpm.php (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 53 | Error | exec-sin-escapeshellarg | exec()/shell_exec() sin escapeshellarg(). Riesgo de inyeccion de comandos. |

---

## App/React/islands/social/PerfilIsland.tsx (9 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 6 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 85 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 86 | Warning | zustand-sin-selector | useTabsTopBarStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 88 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 90 | Warning | zustand-sin-selector | useConfiguracionModalStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 91 | Warning | zustand-sin-selector | useChatFlotanteStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 118 | Warning | useeffect-sin-cleanup | useEffect con async/fetch sin AbortController en cleanup. Puede causar updates en componentes desmontados. |
| 162 | Warning | useeffect-sin-cleanup | useEffect con async/fetch sin AbortController en cleanup. Puede causar updates en componentes desmontados. |
| 463 | Warning | limite-lineas | Archivo excede limite de 300 lineas para componente (398 lineas efectivas). Dividir obligatoriamente. |

---

## App/React/components/layout/TopBar.tsx (9 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 6 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 30 | Warning | zustand-sin-selector | useTabsTopBarStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 31 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 32 | Warning | zustand-sin-selector | useFiltrosStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 33 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 34 | Warning | zustand-sin-selector | useCrearModalStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 35 | Warning | zustand-sin-selector | useConfiguracionModalStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 36 | Warning | zustand-sin-selector | usePlanesModalStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 37 | Warning | zustand-sin-selector | usePanelLateralStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/islands/samples/SampleDetalleIsland.tsx (7 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 12 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 71 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 72 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 75 | Warning | zustand-sin-selector | usePlanesModalStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 110 | Warning | zustand-sin-selector | usePanelLateralStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 212 | Warning | useeffect-sin-cleanup | useEffect con async/fetch sin AbortController en cleanup. Puede causar updates en componentes desmontados. |
| 682 | Warning | limite-lineas | Archivo excede limite de 300 lineas para componente (590 lineas efectivas). Dividir obligatoriamente. |

---

## App/React/islands/colecciones/ColeccionDetalleIsland.tsx (7 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 5 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 41 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 42 | Warning | zustand-sin-selector | useTabsTopBarStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 43 | Warning | zustand-sin-selector | usePanelLateralStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 44 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 66 | Warning | useeffect-sin-cleanup | useEffect con async/fetch sin AbortController en cleanup. Puede causar updates en componentes desmontados. |
| 390 | Warning | limite-lineas | Archivo excede limite de 300 lineas para componente (337 lineas efectivas). Dividir obligatoriamente. |

---

## App/React/islands/libreria/LibreriaIsland.tsx (6 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 6 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 52 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 53 | Warning | zustand-sin-selector | useSubirModalStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 54 | Warning | zustand-sin-selector | useTabsTopBarStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 58 | Warning | zustand-sin-selector | usePanelLateralStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 100 | Warning | useeffect-sin-cleanup | useEffect con async/fetch sin AbortController en cleanup. Puede causar updates en componentes desmontados. |

---

## App/React/components/social/ChatFlotante.tsx (6 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 4 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 23 | Warning | zustand-sin-selector | useChatFlotanteStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 24 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 25 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 36 | Warning | useeffect-sin-cleanup | useEffect con async/fetch sin AbortController en cleanup. Puede causar updates en componentes desmontados. |
| 320 | Warning | zustand-sin-selector | useChatFlotanteStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/islands/mensajes/ChatIsland.tsx (5 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 4 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 72 | Warning | zustand-sin-selector | useMensajesStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 74 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 75 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 97 | Warning | useeffect-sin-cleanup | useEffect con async/fetch sin AbortController en cleanup. Puede causar updates en componentes desmontados. |

---

## App/React/components/social/ModalConfiguracion.tsx (5 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 10 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 28 | Warning | zustand-sin-selector | usePanelLateralStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 76 | Warning | zustand-sin-selector | useConfiguracionModalStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 77 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 451 | Warning | limite-lineas | Archivo excede limite de 300 lineas para componente (393 lineas efectivas). Dividir obligatoriamente. |

---

## App/React/components/feed/PanelLibreria.tsx (5 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 7 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 39 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 40 | Warning | zustand-sin-selector | useSubirModalStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 41 | Warning | zustand-sin-selector | usePanelLateralStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 44 | Warning | useeffect-sin-cleanup | useEffect con async/fetch sin AbortController en cleanup. Puede causar updates en componentes desmontados. |

---

## App/React/components/feed/PanelDetalleSample.tsx (5 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 9 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 40 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 41 | Warning | zustand-sin-selector | usePanelLateralStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 86 | Warning | useeffect-sin-cleanup | useEffect con async/fetch sin AbortController en cleanup. Puede causar updates en componentes desmontados. |
| 399 | Warning | limite-lineas | Archivo excede limite de 300 lineas para componente (341 lineas efectivas). Dividir obligatoriamente. |

---

## App/React/islands/planes/PlanesIsland.tsx (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 4 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 115 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 116 | Warning | zustand-sin-selector | usePlanesModalStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 124 | Warning | useeffect-sin-cleanup | useEffect con async/fetch sin AbortController en cleanup. Puede causar updates en componentes desmontados. |

---

## App/React/islands/feed/InicioIsland.tsx (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 30 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 56 | Warning | zustand-sin-selector | useCrearModalStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 57 | Warning | zustand-sin-selector | useFiltrosStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 58 | Warning | zustand-sin-selector | usePanelLateralStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/islands/comunidad/ComunidadIsland.tsx (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 5 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 90 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 91 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 419 | Warning | limite-lineas | Archivo excede limite de 300 lineas para componente (364 lineas efectivas). Dividir obligatoriamente. |

---

## App/React/islands/admin/DashboardCreadorIsland.tsx (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 9 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 271 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 279 | Warning | useeffect-sin-cleanup | useEffect con async/fetch sin AbortController en cleanup. Puede causar updates en componentes desmontados. |
| 510 | Warning | limite-lineas | Archivo excede limite de 300 lineas para componente (452 lineas efectivas). Dividir obligatoriamente. |

---

## App/React/components/ui/TarjetaSample.tsx (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 4 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 90 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 92 | Warning | useeffect-sin-cleanup | useEffect con async/fetch sin AbortController en cleanup. Puede causar updates en componentes desmontados. |
| 559 | Warning | limite-lineas | Archivo excede limite de 300 lineas para componente (439 lineas efectivas). Dividir obligatoriamente. |

---

## App/React/components/feed/FeedSamples.tsx (4 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 7 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 127 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 392 | Warning | zustand-sin-selector | usePanelLateralStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 602 | Warning | limite-lineas | Archivo excede limite de 300 lineas para componente (466 lineas efectivas). Dividir obligatoriamente. |

---

## App/React/islands/social/EditarPerfilIsland.tsx (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 8 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 30 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 36 | Warning | useeffect-sin-cleanup | useEffect con async/fetch sin AbortController en cleanup. Puede causar updates en componentes desmontados. |

---

## App/React/islands/samples/SamplesIsland.tsx (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 5 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 44 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 45 | Warning | zustand-sin-selector | useTabsTopBarStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/islands/libreria/FavoritosIsland.tsx (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 29 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 30 | Warning | zustand-sin-selector | useTabsTopBarStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 31 | Warning | zustand-sin-selector | usePanelLateralStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/islands/libreria/DescargasIsland.tsx (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 29 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 30 | Warning | zustand-sin-selector | useTabsTopBarStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 31 | Warning | zustand-sin-selector | usePanelLateralStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/ui/SubirModal.tsx (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 6 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 43 | Warning | zustand-sin-selector | useSubirModalStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 44 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/social/ModalSeleccionColeccion.tsx (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 6 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 25 | Warning | zustand-sin-selector | useColeccionPickerStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 39 | Warning | useeffect-sin-cleanup | useEffect con async/fetch sin AbortController en cleanup. Puede causar updates en componentes desmontados. |

---

## App/React/components/social/ListaComentarios.tsx (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 9 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 183 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 455 | Warning | limite-lineas | Archivo excede limite de 300 lineas para componente (410 lineas efectivas). Dividir obligatoriamente. |

---

## App/React/components/social/LandingPublica.tsx (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 29 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 31 | Warning | zustand-sin-selector | useReproductorStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 34 | Warning | useeffect-sin-cleanup | useEffect con async/fetch sin AbortController en cleanup. Puede causar updates en componentes desmontados. |

---

## App/React/components/layout/Sidebar.tsx (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 52 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 53 | Warning | zustand-sin-selector | useConfiguracionModalStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 54 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/Kamples/Api/Controladores/MensajesController.php (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 154 | Warning | json-decode-inseguro | json_decode() sin verificar json_last_error(). Datos corruptos se propagan como null silencioso. |
| 341 | Warning | json-decode-inseguro | json_decode() sin verificar json_last_error(). Datos corruptos se propagan como null silencioso. |
| 437 | Warning | limite-lineas | Archivo excede limite de 300 lineas para controlador (338 lineas efectivas). Dividir obligatoriamente. |

---

## App/Kamples/Api/AnalizadoresModeracion.php (3 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 226 | Warning | json-decode-inseguro | json_decode() sin verificar json_last_error(). Datos corruptos se propagan como null silencioso. |
| 229 | Warning | json-decode-inseguro | json_decode() sin verificar json_last_error(). Datos corruptos se propagan como null silencioso. |
| 296 | Warning | json-decode-inseguro | json_decode() sin verificar json_last_error(). Datos corruptos se propagan como null silencioso. |

---

## App/React/islands/player/ReproductorIsland.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 25 | Warning | zustand-sin-selector | useReproductorStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 27 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/islands/notificaciones/NotificacionesIsland.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 74 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 76 | Warning | useeffect-sin-cleanup | useEffect con async/fetch sin AbortController en cleanup. Puede causar updates en componentes desmontados. |

---

## App/React/islands/mensajes/MensajesIsland.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 44 | Warning | zustand-sin-selector | useMensajesStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 46 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/islands/explorador/ExploradorIsland.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 31 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 32 | Warning | zustand-sin-selector | usePanelLateralStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/islands/discover/DescubrirIsland.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 30 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 73 | Warning | useeffect-sin-cleanup | useEffect con async/fetch sin AbortController en cleanup. Puede causar updates en componentes desmontados. |

---

## App/React/components/ui/DropdownMensajes.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 35 | Warning | zustand-sin-selector | useChatFlotanteStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 43 | Warning | zustand-sin-selector | useMensajesStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/ui/ContenedorToasts.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 20 | Warning | zustand-sin-selector | useToastStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 67 | Warning | zustand-sin-selector | useToastStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/ui/BotonDevTools.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 31 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 32 | Warning | zustand-sin-selector | useDevToolsStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/social/ModalPublicar.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 20 | Warning | zustand-sin-selector | usePublicarModalStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 21 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/social/ModalCrear.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 13 | Warning | zustand-sin-selector | useCrearModalStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 14 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/social/FilaColecciones.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 18 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 20 | Warning | useeffect-sin-cleanup | useEffect con async/fetch sin AbortController en cleanup. Puede causar updates en componentes desmontados. |

---

## App/React/components/social/ComentarioItem.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 5 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 57 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/feed/PanelSugerencias.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 22 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 23 | Warning | zustand-sin-selector | usePanelLateralStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/feed/ModalSugerenciasLike.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 19 | Warning | zustand-sin-selector | useSugerenciasLikeStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 20 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/auth/ModalAuth.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 5 useState detectados (max 3). Extraer logica a un hook personalizado. |
| 152 | Warning | zustand-sin-selector | useAuthModalStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/auth/InicializadorAuth.tsx (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 38 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |
| 40 | Warning | useeffect-sin-cleanup | useEffect con async/fetch sin AbortController en cleanup. Puede causar updates en componentes desmontados. |

---

## App/Kamples/Api/Controladores/PagosController.php (2 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 159 | Warning | json-decode-inseguro | json_decode() sin verificar json_last_error(). Datos corruptos se propagan como null silencioso. |
| 200 | Warning | controller-sin-trycatch | Metodo publico "listarPlanes" sin try-catch global. Envolver cuerpo completo en try { ... } catch (\Throwable $e). |

---

## Mezclador/hooks/useTimeline.ts (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 211 | Warning | limite-lineas | Archivo excede limite de 120 lineas para hook (160 lineas efectivas). Dividir obligatoriamente. |

---

## Mezclador/hooks/usePianoRoll.ts (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 346 | Warning | limite-lineas | Archivo excede limite de 120 lineas para hook (275 lineas efectivas). Dividir obligatoriamente. |

---

## Mezclador/hooks/useMotorAudio.ts (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 333 | Warning | limite-lineas | Archivo excede limite de 120 lineas para hook (240 lineas efectivas). Dividir obligatoriamente. |

---

## App/React/islands/dev/secciones/ShowcaseFormularios.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 4 useState detectados (max 3). Extraer logica a un hook personalizado. |

---

## App/React/islands/auth/RegistroIsland.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 5 useState detectados (max 3). Extraer logica a un hook personalizado. |

---

## App/React/islands/admin/AdminPanelIsland.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 26 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/hooks/useSamples.ts (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 154 | Warning | limite-lineas | Archivo excede limite de 120 lineas para hook (123 lineas efectivas). Dividir obligatoriamente. |

---

## App/React/hooks/useMenuContextualSample.ts (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 263 | Warning | limite-lineas | Archivo excede limite de 120 lineas para hook (231 lineas efectivas). Dividir obligatoriamente. |

---

## App/React/hooks/useExploradorPagina.ts (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 151 | Warning | limite-lineas | Archivo excede limite de 120 lineas para hook (132 lineas efectivas). Dividir obligatoriamente. |

---

## App/React/hooks/useEditar.ts (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 204 | Warning | limite-lineas | Archivo excede limite de 120 lineas para hook (171 lineas efectivas). Dividir obligatoriamente. |

---

## App/React/hooks/useCrearContenido.ts (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 267 | Warning | limite-lineas | Archivo excede limite de 120 lineas para hook (219 lineas efectivas). Dividir obligatoriamente. |

---

## App/React/hooks/useComentarios.ts (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 247 | Warning | limite-lineas | Archivo excede limite de 120 lineas para hook (212 lineas efectivas). Dividir obligatoriamente. |

---

## App/React/hooks/useArchivosDragDrop.ts (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 167 | Warning | limite-lineas | Archivo excede limite de 120 lineas para hook (133 lineas efectivas). Dividir obligatoriamente. |

---

## App/React/hooks/useAdminPanel.ts (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 162 | Warning | limite-lineas | Archivo excede limite de 120 lineas para hook (132 lineas efectivas). Dividir obligatoriamente. |

---

## App/React/components/ui/ReproductorGlobal.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 22 | Warning | zustand-sin-selector | useReproductorStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/ui/ModalFiltros.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 41 | Warning | zustand-sin-selector | useFiltrosStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/ui/DropdownNotificaciones.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 44 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/ui/BotonExperimentos.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 16 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/social/TarjetaPublicacion.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 69 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/social/SeccionPublicar.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 18 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/social/ModalEditar.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 54 | Warning | zustand-sin-selector | useEditarModalStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/social/ModalColeccion.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 4 useState detectados (max 3). Extraer logica a un hook personalizado. |

---

## App/React/components/social/EnlaceCreador.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 36 | Warning | zustand-sin-selector | useNavigationStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/social/ContenidoCrear.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 23 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/social/BotonLike.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 1 | Warning | usestate-excesivo | 5 useState detectados (max 3). Extraer logica a un hook personalizado. |

---

## App/React/components/layout/PanelLateral.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 36 | Warning | zustand-sin-selector | usePanelLateralStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/layout/LayoutPrincipal.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 82 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/React/components/auth/ConAutenticacion.tsx (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 28 | Warning | zustand-sin-selector | useAuthStore() sin selector. Re-renderiza en CUALQUIER cambio del store. Usar useStore(s => s.campo). |

---

## App/Kamples/Services/StripeService.php (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 154 | Warning | json-decode-inseguro | json_decode() sin verificar json_last_error(). Datos corruptos se propagan como null silencioso. |

---

## App/Kamples/Api/Helpers/NormalizadorSample.php (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 253 | Warning | limite-lineas | Archivo excede limite de 150 lineas para util (167 lineas efectivas). Dividir obligatoriamente. |

---

## App/Kamples/Api/Controladores/SamplesUploadController.php (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 126 | Warning | json-decode-inseguro | json_decode() sin verificar json_last_error(). Datos corruptos se propagan como null silencioso. |

---

## App/Kamples/Api/Controladores/DescargasStreamController.php (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 23 | Warning | controller-sin-trycatch | Metodo publico "generarFirmaDescarga" sin try-catch global. Envolver cuerpo completo en try { ... } catch (\Throwable $e). |

---

## App/Kamples/Api/Controladores/ComentariosEscrituraController.php (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 181 | Warning | json-decode-inseguro | json_decode() sin verificar json_last_error(). Datos corruptos se propagan como null silencioso. |

---

## App/Kamples/Api/Controladores/ComentariosController.php (1 violaciones)

| Linea | Severidad | Regla | Mensaje |
|-------|-----------|-------|---------|
| 251 | Warning | json-decode-inseguro | json_decode() sin verificar json_last_error(). Datos corruptos se propagan como null silencioso. |

