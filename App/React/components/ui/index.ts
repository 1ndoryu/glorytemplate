// Barrel export para componentes UI
// Permite importar todos los componentes desde un solo punto
// Ejemplo: import { Button, Badge } from '@/components/ui';

export {Badge} from './Badge';
export {Button} from './Button';
